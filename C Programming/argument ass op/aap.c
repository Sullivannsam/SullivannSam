#include <stdio.h>
int main(){

    // argument assignment operator = used to replace a statement where an operator takes a variable as one of its arguments
    // and then assigns the result back to the same variable
    // x = x + 1;
    // x+= 1;

    int a = 10;
    int b = 30;

     a += 5;
    printf("%d", a);
    int e = 20;
    e /= 3;
    printf("\n%d", e);






    return 0;
}